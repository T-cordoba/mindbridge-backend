apiVersion: v1
kind: Service
metadata:
  name: mindbridge-frontend
  namespace: mindbridge
spec:
  type: NodePort
  selector:
    app: mindbridge-frontend
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30000
